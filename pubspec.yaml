name: portable_ai_flutter
description: "Krem|Ai — private local-first GGUF chat with optional internet context, photo, and file attachments."
publish_to: 'none'
version: 1.2.0+3

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  # ── LLM Engine ──────────────────────────────────────────────────
  llamadart: ^0.6.10

  # ── State Management ────────────────────────────────────────────
  get: ^4.7.2                      # GetX - controllers, routing, state

  # ── Local Storage ───────────────────────────────────────────────
  hive_flutter: ^1.1.0             # Hive with Flutter init helpers
  hive: ^2.2.3                     # Hive core key-value + box storage

  # ── Networking / Downloads ──────────────────────────────────────
  http: ^1.2.2                     # HTTP requests + model downloading

  # ── File System ─────────────────────────────────────────────────
  path_provider: ^2.1.5            # App documents / downloads dir
  path: ^1.9.1                     # Path string utilities
  file_picker: ^8.1.7              # Cross-platform file/folder picker
  image_picker: ^1.1.2             # iOS photo library and camera access
  url_launcher: ^6.3.1             # Open Krem|Ai key and payment pages

  # ── UI Helpers ───────────────────────────────────────────────────
  flutter_markdown: ^0.7.6         # Render AI markdown responses
  google_fonts: ^6.2.1             # Inter font (matching FastChatUI)
  percent_indicator: ^4.2.4        # Download progress bars
  flutter_animate: ^4.5.0          # Micro-animations
  cupertino_icons: ^1.0.8

  # ── Background / Wake Lock ─────────────────────────────────────
  wakelock_plus: ^1.2.10             # Keep screen on during downloads
  flutter_foreground_task: ^9.2.2    # Foreground service for downloads/inference
  disable_battery_optimization: ^1.1.1 # Guide users to disable battery optimization

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1           # Hive type adapter code gen
  build_runner: ^2.4.13            # Code generation runner
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4

hooks:
  user_defines:
    llamadart:
      llamadart_native_backends:
        platforms:
          linux-x64: [vulkan]
          windows-x64: [vulkan]
          android-arm64: [cpu, vulkan, opencl]

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/branding/devhub_kremityss_logo.png
  remove_alpha_ios: true

flutter_native_splash:
  color: "#080808"
  color_dark: "#080808"
  image: assets/branding/devhub_kremityss_logo.png
  image_dark: assets/branding/devhub_kremityss_logo.png
  android_12:
    color: "#080808"
    color_dark: "#080808"
    image: assets/branding/devhub_kremityss_logo.png
    image_dark: assets/branding/devhub_kremityss_logo.png
  ios: true
  android: true
  web: false

flutter:
  uses-material-design: true
  assets:
    - assets/models_catalog.json
    - assets/branding/devhub_kremityss_logo.png
    - assets/branding/kremityss_ai_logo.png
